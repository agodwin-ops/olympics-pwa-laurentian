# Security Configuration Template
# Copy this to .env and set appropriate values for production

# Environment (development/production)
ENVIRONMENT=production

# JWT Security
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=480  # 8 hours

# Admin Security
ADMIN_CODE=your-super-secret-admin-code-change-this

# CORS Configuration (production only)
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Database Security
DATABASE_URL=your-secure-database-url

# API Security
DEBUG=false
PORT=8000

# Rate Limiting (configured in code but can be environment-based)
# Auth endpoints: 5 registration/minute, 10 login/minute, 3 admin-check/minute
# Admin endpoints: 30 award/minute, 10 bulk-award/minute, 5 reset/minute